# """Task 6"""

# password = 'python123'
# user_input = input('Enter password You got 3 attempts: ')

# if user_input == password:
#     print('Access granted')
# else:
#     print('You have left 2 more attempts')
#     user_input = input('Enter password: ')
#     print('You have left 1 more attempt')
#     user_input = input('Enter password: ')
#     print('Access denied')


"""Task 6 (improved version)"""

username = 'admin'
password = 'python123'

user_name = input('Enter username: ')
user_password = input('Enter password You got 3 attempts: ')

if user_name == username and user_password == password:
    print('Access granted')
    print(f'Welcome, {username}')
else:
    print('You have left 2 more attempts')
    user_password = input('Enter password: ')
    if user_name == username and user_password == password:
        print(f'Welcome, {username}')
    else:
        print('You have left 1 more attempt')
        user_password = input('Enter password: ')
        if user_name == username and user_password == password:
            print(f'Welcome, {username}')
        else:   
            print('Invalid username or password')